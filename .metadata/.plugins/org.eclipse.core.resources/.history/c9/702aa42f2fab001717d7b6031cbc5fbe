package it.cast.cn.day37.demo01.copy;

import java.io.IOException;

import javax.servlet.ServletException;
import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

public class TestWriteCookieServlet extends HttpServlet {

	@Override
	protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		System.out.println("请求到达  test write cookie servlet");
		//创建Cookie对象
		Cookie cookie = new Cookie("testCookie","hello Cookie");
		//设置属性
		//设置有限期,单位为秒
		cookie.setMaxAge(200);
		//设置有限路径,只有访问此路径的时候才会携带Cookie
		cookie.setPath(req.getContextPath()+"/ddd/");
		//写入Cookie
		resp.addCookie(cookie);
		
	}

	@Override
	protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
		doPost(req, resp);
	}
	

}
